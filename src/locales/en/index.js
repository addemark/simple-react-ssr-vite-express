import LoginPage from "./Login.Page.json" with { type: "json" };
import Form from "./Form.json" with { type: "json" };
import Recorder from "./Recorder.json" with { type: "json" };
import CreateReview from "./CreateReview.Page.json" with { type: "json" };

const en = {
  LoginPage,
  Form,
  Recorder,
  CreateReview,
};

export default en;
